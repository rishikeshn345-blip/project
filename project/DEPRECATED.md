This nested folder contains an older duplicate frontend (home.html/home.js/home.css). It is deprecated. Use the root-level `index.html`, `client.js`, and `responce.html` files instead.

You can safely remove this folder if you have a backup. It remains here to avoid accidental data loss.
