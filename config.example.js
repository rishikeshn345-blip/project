// Copy this file to config.js and fill in your Google API key.
// WARNING: Placing API keys in a client-side file exposes them to anyone who opens the page.
// Use this only for quick local testing. For production, use a server proxy or service account.

// Example (create config.js in the same folder):
// window.GOOGLE_API_KEY = 'REPLACE_WITH_YOUR_KEY';
// window.MODEL = 'models/text-bison-001'; // optional, change if you have a Gemini model id

// No code here — copy to config.js and uncomment and set values.
